import { createContext } from 'react';

const PersonCentext = createContext(null);
const MainContext = createContext(null);
const SettingContext = createContext(null);
const FevContext = createContext(null);

export {
  PersonCentext,
  MainContext,
  SettingContext,
  FevContext
}