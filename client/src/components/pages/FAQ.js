import React, { Fragment, useContext } from 'react';

import { BackButton } from '../widgets';

const FAQ = () => {
  return (
    <Fragment>
      <BackButton />
    </Fragment>
  )
}
export default FAQ;